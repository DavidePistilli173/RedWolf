[APPLICATION]
cycle_duration = 1 # Duration of each loop cycle in seconds.
total_iterations = 40 # Iterations after which the program stops.

[TIMER]
frequency = 60.0 # Frequency in Hertz.

[SOCKET]
local_address = 127.0.0.1 # Local address to bind the socket to.
local_port = 1234 # Local port to bind the socket to.